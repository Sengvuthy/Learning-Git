spring:
    datasource:
        url: jdbc:h2:mem:testdb
        username: sa
        password: 
    jpa:
        Properties:
            hibernate:
            dialect: org.hibernate.dialect.H2Dialect
        hibernate:
            ddl-auto: create